% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inference.R
\name{Identify_neighbor_list}
\alias{Identify_neighbor_list}
\title{Identify spatial neighbors}
\usage{
Identify_neighbor_list(
  locs,
  senders = NULL,
  receivers = NULL,
  dist_threshold = 5,
  self_neighbor = F,
  decay_type = NULL,
  decay_threshold = 0.001,
  normalize = T,
  nclusters = 1
)
}
\arguments{
\item{locs}{A data frame containing exactly two spatial-coordinate columns.
Row names must contain the cell identifiers.}

\item{senders}{Optional character vector of cells eligible to be senders.
Defaults to all cells in \code{locs}.}

\item{receivers}{Optional character vector of receiver cells. Defaults to all
cells in \code{locs}.}

\item{dist_threshold}{Numeric maximum distance at which a sender is considered
a neighbor.}

\item{self_neighbor}{Logical; whether a cell may be its own neighbor.}

\item{decay_type}{Optional character scalar specifying the decay rule. Accepted
abbreviations are \code{"exp"}, \code{"rec"}, \code{"insq"}, and \code{"rbf"}; other values
use constant weights.}

\item{decay_threshold}{Numeric relative-weight threshold used to discard weak
neighbors when \code{normalize = FALSE}.}

\item{normalize}{Logical; whether each receiver's neighbor weights should sum
to one.}

\item{nclusters}{Integer number of parallel workers, or \code{NULL} to choose a
value from the available cores.}
}
\value{
A named list with one numeric weight vector per receiver. Vector names
identify sender cells.
}
\description{
Construct a weighted sender-neighbor vector for every receiver cell using
spatial distances and a selected distance-decay rule.
}
